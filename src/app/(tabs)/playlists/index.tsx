import { View, Text } from 'react-native'
import { defaultStyles } from '@/styles'

const PlaylistsScreen = () => {
	return (
		<View style={defaultStyles.container}>
			<Text style={defaultStyles.text}>play list</Text>
		</View>
	)
}

export default PlaylistsScreen
